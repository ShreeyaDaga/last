Terminal 1 →
cd /src
javac *.java
rmiregistry

Terminal 2 →
cd src
java DistServer

Terminal 3 →
cd src
java DistClient 127.0.0.1 10
        # 127.0.0.1 → local host IP, if on different machines, then put the IP of that machine


If some error comes up 
After fixing it →
rm *.class
javac *.java
